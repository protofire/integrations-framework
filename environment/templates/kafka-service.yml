#apiVersion: v1
#kind: Service
#metadata:
#  name: kafka-service
#  labels:
#    name: kafka
metadata:
  name: kafka-pod
spec:
  ports:
    - name: kafka-port
      port: 9092
      targetPort: 9092
  selector:
    app: kafka-pod
  type: ClusterIP